import styles from "../../../styles/Home.module.css"

const Login = () => {
  return <div className={styles.main}>You must be logged in, in order to access this feature</div>;
};

export default Login;