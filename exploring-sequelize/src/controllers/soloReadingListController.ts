import { Op } from 'sequelize';
import { Request, Response } from 'express';
import { User, FriendRequest } from '../models/index';
import { AppError } from '../utils/appError';

export class SoloReadingListController {
    static async createReadingList(req: Request, res: Response) {

    }

    static async addReadingList(req: Request, res: Response) {

    }

    static async updateReadingList(req: Request, res: Response) {

    }

    static async getReadingLists(req: Request, res: Response) {

    }
}
